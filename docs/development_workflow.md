one plan at a time
codex branch only
master protected
new code file = matching .md file
    Every new .py file requires a matching .md file.
    Every changed .py file requires matching .md update if behavior changes.
one logical change = one commit
next plan requires user confirmation