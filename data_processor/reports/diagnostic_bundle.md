# diagnostic_bundle.py

## Purpose

`diagnostic_bundle.py` creates one complete diagnostic report from a processed `Table`.

It belongs to the report layer and aggregates existing diagnostic sections without modifying data.

---

## Architecture

```text
Table + Validation Results
→ Diagnostic Bundle
→ Complete Report Dictionary
```

---

## Main Function

### `build_diagnostic_bundle(table, validation_results=None)`

Returns a dictionary with these top-level sections:

```python
{
    "table_name": "...",
    "row_count": ...,
    "column_count": ...,
    "metadata": {...},
    "parse_diagnostics": {...},
    "quality_report": {...},
    "column_profiles": {...},
    "row_profiles": [...],
    "row_classification": {...},
    "type_diagnostics": {...},
    "validation_report": {...},
    "quarantine_candidates": {...}
}
```

---

## `parse_diagnostics`

Describes source parsing observations such as short rows, extra fields, duplicate headers, empty headers, detected delimiter, and detected encoding.

---

## `quality_report`

Includes missing values, duplicate rows, empty columns, and high-null columns.

---

## `column_profiles`

Includes column-level profile data.

---

## `row_profiles`

Includes row-level profile data.

---

## `row_classification`

Generated by:

```python
classify_table_rows(table)
```

Includes:

```text
rows
suspicious_rows
summary
```

This section reports suspicious rows such as empty rows, comment rows, summary rows, footer rows, and garbage rows.

---

## `type_diagnostics`

Generated by:

```python
analyze_table_type_evidence(table)
```

Includes:

```text
columns
mixed_type_columns
```

This section reports mostly consistent columns that contain incompatible values.

---

## `validation_report`

Generated by:

```python
generate_validation_report(validation_results)
```

If no validation results are provided, the validation report is included but empty.

---

## `quarantine_candidates`

Generated by:

```python
build_quarantine_candidates(
    table_rows=table.rows,
    row_classification=row_classification,
    type_diagnostics=type_diagnostics,
    validation_report=validation_report,
)
```

Includes:

```text
candidate_count
summary
candidates
```

This section groups row-level review signals from existing diagnostics.

Sources:

```text
row_classification
type_diagnostics
validation_report
```

Important:

```text
Quarantine candidates do not remove rows or change CSV export behavior.
```

---

## Design Rules

Diagnostic bundles aggregate reports only.

They must not:

- clean data
- cast values
- validate constraints directly
- modify rows
- remove rows
- export files

Exporting belongs to report exporters.

---

## Developer Notes

This module should stay small and avoid duplicating report logic.

Future additions may include timestamps, runtime duration, report versioning, source metadata, quality scoring, and explicit quarantine export support.
