# JSON CLI Tests

Tests for the JSON pipeline command-line script.

Covered behavior:

```text
clean CSV output
console completion message
JSON report output
HTML report output
```

Run:

```bash
python -m pytest tests/test_cli_json_pipeline.py
```
